<%= form_for @room do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <%= f.submit 'Save', class: 'btn btn-success' %>
<% end %>